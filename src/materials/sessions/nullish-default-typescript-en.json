{
  "id": "nullish-default-typescript-en",
  "title": "Tell ?? apart from ||",
  "language": "en",
  "difficulty": "easy",
  "codeLanguage": "typescript",
  "summary": "Fix the default value spots that strictNullChecks turned red.",
  "files": [
    {
      "path": "src/config/settings.ts",
      "content": "export type Settings = {\n  retry?: { count?: number }\n  label?: string\n}\n\nexport function retryCount(settings: Settings): number {\n  return settings.retry.count || 3\n}\n\nexport function label(settings: Settings): string {\n  return settings.label || 'untitled'\n}\n"
    }
  ],
  "turns": [
    {
      "user": "turned on strictNullChecks and settings.retry.count is red now. just make it compile",
      "assistant": [
        {
          "kind": "text",
          "body": "Let's guard the front with optional chaining, since retry is optional. count can legitimately be 0, so the default goes through ?? rather than ||."
        },
        {
          "kind": "code",
          "lang": "typescript",
          "body": "export function retryCount(settings: Settings): number {\n  return settings.retry?.count ?? 3\n}\n\nexport function label(settings: Settings): string {\n  return settings.label ?? 'untitled'\n}\n"
        }
      ]
    },
    {
      "user": "wait sorry, label should fall back to untitled on an empty string too. count still needs 0 to survive",
      "assistant": [
        {
          "kind": "text",
          "body": "|| rejects every falsy value, which is exactly what label wants and exactly what count does not. Note: ?? only ever looks at null and undefined."
        },
        {
          "kind": "code",
          "lang": "typescript",
          "body": "export function retryCount(settings: Settings): number {\n  return settings.retry?.count ?? 3\n}\n\nexport function label(settings: Settings): string {\n  return settings.label || 'untitled'\n}\n"
        }
      ]
    }
  ]
}
